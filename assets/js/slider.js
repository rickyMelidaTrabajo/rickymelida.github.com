

$(document).ready(function() {
    $("#content-slider").lightSlider({
        auto: true,
        item:4,
        loop:true,
        slideMove:2,
        easing: 'cubic-bezier(0.25, 0, 0.25, 1)',
        speed:600,
    });  
  });